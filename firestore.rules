rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Any authenticated user can read and write to user profiles.
    match /users/{userId} {
      allow read, write: if request.auth != null;
    }

    // Any authenticated user to read and write to devices.
    match /devices/{deviceId} {
      allow read, write: if request.auth != null;
    }
  }
}
